--- a/src/egl/meson.build
+++ b/src/egl/meson.build
@@ -148,7 +148,7 @@
   incs_for_egl += [include_directories('../gallium/frontends/hgl')]
 
   link_for_egl += [libmesa, libgallium, libswhgl, libsthgl, libglapi]
-  deps_for_egl += [cpp.find_library('be'), driver_swrast, driver_panfrost, idep_xmlconfig]
+  deps_for_egl += [cpp.find_library('be'), driver_swrast, driver_panfrost, driver_zink, idep_xmlconfig]
 elif with_platform_windows
   c_args_for_egl += [
     '-DEGLAPI=', '-DPUBLIC='
--- a/src/egl/drivers/haiku/egl_haiku.cpp
+++ b/src/egl/drivers/haiku/egl_haiku.cpp
@@ -478,7 +478,17 @@
    {
       struct sw_winsys *winsys = hgl_create_sw_winsys();
       if (winsys) {
-         screen = sw_screen_create(winsys);
+#ifdef GALLIUM_ZINK
+         /* A discrete GPU is only reachable through Vulkan on Haiku: it has no
+          * Gallium kernel driver, but it does have a Vulkan one (NVK on
+          * NVIDIA). Zink turns GL and GLES into Vulkan calls; the software
+          * rasterisers below stay the fallback. */
+         if (!disp->Options.ForceSoftware
+             && !debug_get_bool_option("LIBGL_ALWAYS_SOFTWARE", false))
+            screen = sw_screen_create_zink(winsys, NULL, false);
+#endif
+         if (!screen)
+            screen = sw_screen_create(winsys);
          if (!screen)
             winsys->destroy(winsys);
       }
--- a/src/gallium/auxiliary/target-helpers/inline_sw_helper.h
+++ b/src/gallium/auxiliary/target-helpers/inline_sw_helper.h
@@ -29,6 +29,12 @@
 #include "d3d12/d3d12_public.h"
 #endif
 
+/* sw_helper.h declares zink the same way; this header left it out, so every
+ * target that reaches zink through it failed to compile. */
+#ifdef GALLIUM_ZINK
+#include "zink/zink_public.h"
+#endif
+
 static inline struct pipe_screen *
 sw_screen_create_named(struct sw_winsys *winsys, const char *driver)
 {
