{
  "manifest_version": 2,
  "name": "Summit blocking webRequest fixture",
  "version": "1.0",
  "description": "Cancels, redirects and rewrites requests to the local webrequest-blocking fixture server.",
  "permissions": ["webRequest", "webRequestBlocking", "http://127.0.0.1/*", "http://localhost/*"],
  "background": {"page": "background.html", "persistent": true}
}
